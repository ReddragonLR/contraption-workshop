import type { SolutionPart } from './types';

export const levelId = 'level-07';
// Drop the bowling ball onto the shelf beside the perched weight: it wedges
// the weight off the edge, and the falling weight hoists the roped tennis ball.
export const solution: SolutionPart[] = [{ partId: 'bowling-ball', x: 220, y: 80 }];
